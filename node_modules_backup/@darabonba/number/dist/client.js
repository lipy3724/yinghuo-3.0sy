"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Client {
    static parseInt(raw) {
        if (raw.match(/^\d*$/)) {
            return parseInt(raw);
        }
        else {
            throw "convert int err,raw is not pure int numbers.";
        }
    }
    static parseLong(raw) {
        if (raw.match(/^\d*$/)) {
            return parseInt(raw);
        }
        else {
            throw "convert long err,raw is not pure int numbers.";
        }
    }
    static parseFloat(raw) {
        if (raw.match(/^[\.\d]*$/)) {
            return parseFloat(raw);
        }
        else {
            throw "convert float err,raw is not pure numbers.";
        }
    }
    static parseDouble(raw) {
        if (raw.match(/^[\.\d]*$/)) {
            return parseFloat(raw);
        }
        else {
            throw "convert double err,raw is not pure numbers.";
        }
    }
    static itol(raw) {
        return raw;
    }
    static ltoi(raw) {
        return raw;
    }
    static add(val1, val2) {
        return val1 + val2;
    }
    static sub(val1, val2) {
        return val1 - val2;
    }
    static mul(val1, val2) {
        return val1 * val2;
    }
    static div(val1, val2) {
        return val1 / val2;
    }
    static gt(val1, val2) {
        return val1 > val2;
    }
    static gte(val1, val2) {
        return val1 >= val2;
    }
    static lt(val1, val2) {
        return val1 < val2;
    }
    static lte(val1, val2) {
        return val1 <= val2;
    }
}
exports.default = Client;
//# sourceMappingURL=client.js.map